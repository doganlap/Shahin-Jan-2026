@page "/audits"
@using GrcMvc.Models.Dtos

<div class="container-fluid py-4">
    <div class="row mb-4">
        <div class="col">
            <h1 class="h3">Audits</h1>
        </div>
        <div class="col-auto">
            <a href="/audits/create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> New Audit
            </a>
        </div>
    </div>

    @if (audits == null || audits.Count == 0)
    {
        <div class="alert alert-info">No audits found</div>
    }
    else
    {
        <div class="table-responsive">
            <table class="table table-hover">
                <thead class="table-light">
                    <tr>
                        <th>Number</th>
                        <th>Title</th>
                        <th>Type</th>
                        <th>Status</th>
                        <th>Lead Auditor</th>
                        <th>Start Date</th>
                        <th>Findings</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var audit in audits)
                    {
                        <tr>
                            <td><span class="badge bg-secondary">@audit.AuditNumber</span></td>
                            <td><strong>@audit.Title</strong></td>
                            <td>@audit.Type</td>
                            <td>
                                <span class="badge bg-@GetStatusColor(audit.Status)">
                                    @audit.Status
                                </span>
                                @if (audit.IsOverdue)
                                {
                                    <span class="badge bg-danger ms-2">OVERDUE</span>
                                }
                            </td>
                            <td>@audit.LeadAuditor</td>
                            <td>@audit.PlannedStartDate.ToString("MMM dd, yyyy")</td>
                            <td><span class="badge bg-light text-dark">@audit.FindingCount findings</span></td>
                            <td>
                                <a href="/audits/@audit.Id/edit" class="btn btn-sm btn-outline-primary">Edit</a>
                                <a href="/audits/@audit.Id/detail" class="btn btn-sm btn-outline-secondary">View</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
</div>

@code {
    private List<AuditListItemDto>? audits;

    protected override Task OnInitializedAsync()
    {
        // TODO: Load from service - for now, demo data
        audits = new List<AuditListItemDto>
{
new() { Id = Guid.NewGuid(), AuditNumber = "AUD-INT-001", Title = "Internal IT Audit 2024", Type = "Internal", Status =
"In Progress", LeadAuditor = "Ahmed Al-Mansouri", PlannedStartDate = DateTime.Now.AddDays(-10), PlannedEndDate =
DateTime.Now.AddDays(10), FindingCount = 5, IsOverdue = false },
new() { Id = Guid.NewGuid(), AuditNumber = "AUD-REG-001", Title = "Regulatory Compliance Audit", Type = "Regulatory",
Status = "Planned", LeadAuditor = "Sarah Johnson", PlannedStartDate = DateTime.Now.AddDays(30), PlannedEndDate =
DateTime.Now.AddDays(60), FindingCount = 0, IsOverdue = false },
new() { Id = Guid.NewGuid(), AuditNumber = "AUD-EXT-001", Title = "External Security Audit", Type = "External", Status =
"Completed", LeadAuditor = "Fatima Al-Dosari", PlannedStartDate = DateTime.Now.AddDays(-90), PlannedEndDate =
DateTime.Now.AddDays(-30), ActualStartDate = DateTime.Now.AddDays(-90), ActualEndDate = DateTime.Now.AddDays(-25),
FindingCount = 8, IsOverdue = false }
};
        return Task.CompletedTask;
    }

    private string GetStatusColor(string status) => status switch
    {
        "Completed" => "success",
        "In Progress" => "warning",
        "Planned" => "info",
        "Cancelled" => "danger",
        _ => "secondary"
    };
}