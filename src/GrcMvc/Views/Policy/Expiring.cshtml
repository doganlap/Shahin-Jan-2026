@model IEnumerable<GrcMvc.Models.DTOs.PolicyDto>
@{
    ViewData["Title"] = "Expiring Policies";
    Layout = "_Layout";
}

<div class="container-fluid">
    <div class="row mb-3">
        <div class="col">
            <h2><i class="bi bi-clock-history"></i> Expiring Policies (Next 30 Days)</h2>
        </div>
        <div class="col text-end">
            <a asp-action="Index" class="btn btn-secondary">
                <i class="bi bi-arrow-left"></i> Back to List
            </a>
        </div>
    </div>

    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            @TempData["Error"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <div class="card">
        <div class="card-body">
            @if (!Model.Any())
            {
                <div class="text-center py-5">
                    <i class="bi bi-check-circle display-1 text-muted"></i>
                    <h4 class="mt-3 text-muted">No policies expiring soon.</h4>
                    <p>Everything is up to date!</p>
                </div>
            }
            else
            {
                <div class="table-responsive">
                    <table class="table table-hover" id="expiringTable">
                        <thead>
                            <tr>
                                <th>Expiration Date</th>
                                <th>Policy</th>
                                <th>Owner</th>
                                <th>Status</th>
                                <th>Days Left</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                var daysLeft = (item.ExpirationDate.GetValueOrDefault() - DateTime.Now).Days;
                                var urgencyClass = daysLeft < 7 ? "text-danger fw-bold" : "text-warning";

                                <tr>
                                    <td>@item.ExpirationDate?.ToShortDateString()</td>
                                    <td>
                                        <a asp-action="Details" asp-route-id="@item.Id">@item.Title</a>
                                        <br />
                                        <small class="text-muted">@item.PolicyNumber</small>
                                    </td>
                                    <td>@item.Owner</td>
                                    <td>
                                        <span class="badge @GetStatusBadgeClass(item.Status)">
                                            @item.Status
                                        </span>
                                    </td>
                                    <td class="@urgencyClass">
                                        @if (daysLeft < 0)
                                        {
                                            <span>Expired</span>
                                        }
                                        else
                                        {
                                            <span>@daysLeft days</span>
                                        }
                                    </td>
                                    <td>
                                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-primary">
                                            <i class="bi bi-pencil"></i> Renew / Update
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
        </div>
    </div>
</div>

@functions {
    private string GetStatusBadgeClass(string status)
    {
        return status?.ToLower() switch
        {
            "active" or "published" => "bg-success",
            "draft" => "bg-secondary",
            "review" or "in review" => "bg-warning",
            "archived" or "retired" => "bg-dark",
            "expired" => "bg-danger",
            _ => "bg-info"
        };
    }
}
