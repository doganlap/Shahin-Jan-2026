@model GrcMvc.Models.DTOs.AuditDto
@{
    ViewData["Title"] = "Delete Audit";
    Layout = "_Layout";
}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card border-danger shadow-sm">
                <div class="card-header bg-danger text-white">
                    <h4 class="mb-0"><i class="bi bi-exclamation-triangle"></i> Delete Confirmation</h4>
                </div>
                <div class="card-body">
                    <h5 class="card-title text-danger">Are you sure you want to delete this audit?</h5>
                    <p class="card-text">This action cannot be undone. The audit <strong>@Model.Name (@Model.AuditNumber)</strong> and all its findings will be permanently deleted.</p>

                    <dl class="row mt-4">
                        <dt class="col-sm-4">Audit Number</dt>
                        <dd class="col-sm-8">@Model.AuditNumber</dd>

                        <dt class="col-sm-4">Name</dt>
                        <dd class="col-sm-8">@Model.Name</dd>

                        <dt class="col-sm-4">Type</dt>
                        <dd class="col-sm-8">@Model.Type</dd>

                        <dt class="col-sm-4">Status</dt>
                        <dd class="col-sm-8">@Model.Status</dd>

                        <dt class="col-sm-4">Lead Auditor</dt>
                        <dd class="col-sm-8">@Model.LeadAuditor</dd>
                    </dl>

                    <div class="alert alert-warning">
                        <i class="bi bi-exclamation-circle"></i> This audit has <strong>@Model.FindingsCount</strong> associated findings that will also be deleted.
                    </div>

                    <form asp-action="Delete">
                        <input type="hidden" asp-for="Id" />
                        <div class="d-grid gap-2 d-md-flex justify-content-md-end mt-4">
                            <a asp-action="Index" class="btn btn-secondary me-md-2">Cancel</a>
                            <button type="submit" class="btn btn-danger">
                                <i class="bi bi-trash"></i> Delete Audit
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
