@model GrcMvc.Controllers.TrialRegistrationModel
@using GrcMvc.Services
@inject IAppInfoService AppInfo
@{
    ViewData["Title"] = "ابدأ رحلتك مع " + AppInfo.Name;
    Layout = null; // Full custom page
    
    var hasEmailError = ViewData.ModelState.ContainsKey("Email") && ViewData.ModelState["Email"]?.Errors.Count > 0;
    var emailError = hasEmailError ? ViewData.ModelState["Email"]?.Errors.FirstOrDefault()?.ErrorMessage : null;
    var isAlreadyRegistered = emailError?.Contains("already exists") == true || emailError?.Contains("موجود") == true;
}
<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"]</title>
    
    <!-- Favicon -->
    <link rel="icon" type="image/x-icon" href="~/favicon.ico" />
    <link rel="icon" type="image/png" sizes="32x32" href="~/images/brand/favicon-32.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="~/images/brand/favicon-16.png" />
    <link rel="apple-touch-icon" sizes="128x128" href="~/images/brand/favicon-128.png" />
    
    <!-- Fonts & CSS -->
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@300;400;500;700;800&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css" rel="stylesheet">
    
    <style>
        :root {
            --primary: #6366f1;
            --primary-dark: #4f46e5;
            --secondary: #10b981;
            --accent: #f59e0b;
            --dark: #0f172a;
            --darker: #020617;
            --light: #f8fafc;
            --glass: rgba(255, 255, 255, 0.05);
            --glass-border: rgba(255, 255, 255, 0.1);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Tajawal', sans-serif;
            background: var(--darker);
            color: var(--light);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 2rem;
        }

        /* Background */
        .bg-pattern {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            background: 
                radial-gradient(ellipse at 30% 20%, rgba(99, 102, 241, 0.15) 0%, transparent 50%),
                radial-gradient(ellipse at 70% 80%, rgba(16, 185, 129, 0.1) 0%, transparent 50%),
                var(--darker);
        }

        /* Container */
        .register-container {
            width: 100%;
            max-width: 1100px;
            display: grid;
            grid-template-columns: 1fr 1fr;
            background: var(--glass);
            border: 1px solid var(--glass-border);
            border-radius: 24px;
            overflow: hidden;
            box-shadow: 0 25px 80px rgba(0, 0, 0, 0.5);
        }

        @@media (max-width: 900px) {
            .register-container {
                grid-template-columns: 1fr;
            }
            .register-side {
                display: none;
            }
        }

        /* Left Side - Info */
        .register-side {
            background: linear-gradient(135deg, var(--primary), var(--primary-dark));
            padding: 3rem;
            display: flex;
            flex-direction: column;
            justify-content: center;
            position: relative;
            overflow: hidden;
        }

        .register-side::before {
            content: '';
            position: absolute;
            top: -50%;
            right: -50%;
            width: 100%;
            height: 100%;
            background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, transparent 70%);
        }

        .side-content {
            position: relative;
            z-index: 1;
        }

        .side-logo {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            margin-bottom: 2rem;
        }

        .side-logo-icon {
            width: 50px;
            height: 50px;
            background: rgba(255,255,255,0.2);
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            font-weight: 900;
        }

        .side-logo-text {
            font-size: 1.75rem;
            font-weight: 800;
        }

        .side-title {
            font-size: 2rem;
            font-weight: 800;
            margin-bottom: 1rem;
            line-height: 1.3;
        }

        .side-description {
            font-size: 1.1rem;
            opacity: 0.9;
            margin-bottom: 2rem;
            line-height: 1.7;
        }

        .side-features {
            list-style: none;
        }

        .side-features li {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            margin-bottom: 1rem;
            font-size: 1rem;
        }

        .side-features li i {
            width: 24px;
            height: 24px;
            background: rgba(255,255,255,0.2);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 0.8rem;
        }

        /* Right Side - Form */
        .register-form {
            padding: 3rem;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .form-header {
            text-align: center;
            margin-bottom: 2rem;
        }

        .form-header h1 {
            font-size: 1.75rem;
            font-weight: 800;
            margin-bottom: 0.5rem;
        }

        .form-header p {
            color: rgba(248, 250, 252, 0.6);
        }

        .form-group {
            margin-bottom: 1.25rem;
        }

        .form-group label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: 600;
            font-size: 0.95rem;
        }

        .form-group label i {
            margin-left: 0.5rem;
            color: var(--primary);
        }

        .form-control {
            width: 100%;
            padding: 0.875rem 1rem;
            background: var(--dark);
            border: 1px solid var(--glass-border);
            border-radius: 12px;
            color: var(--light);
            font-size: 1rem;
            font-family: inherit;
            transition: all 0.3s ease;
        }

        .form-control:focus {
            outline: none;
            border-color: var(--primary);
            box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.2);
        }

        .form-control::placeholder {
            color: rgba(248, 250, 252, 0.4);
        }

        .input-group {
            position: relative;
        }

        .input-group .toggle-password {
            position: absolute;
            left: 1rem;
            top: 50%;
            transform: translateY(-50%);
            background: none;
            border: none;
            color: rgba(248, 250, 252, 0.5);
            cursor: pointer;
            padding: 0;
        }

        .form-check {
            display: flex;
            align-items: flex-start;
            gap: 0.75rem;
            margin-bottom: 1.5rem;
        }

        .form-check-input {
            width: 20px;
            height: 20px;
            margin-top: 2px;
            accent-color: var(--primary);
        }

        .form-check-label {
            font-size: 0.9rem;
            color: rgba(248, 250, 252, 0.8);
        }

        .form-check-label a {
            color: var(--primary);
            text-decoration: none;
        }

        .form-check-label a:hover {
            text-decoration: underline;
        }

        .btn-submit {
            width: 100%;
            padding: 1rem;
            background: linear-gradient(135deg, var(--primary), var(--primary-dark));
            border: none;
            border-radius: 12px;
            color: white;
            font-size: 1.1rem;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.75rem;
        }

        .btn-submit:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 30px rgba(99, 102, 241, 0.4);
        }

        .btn-submit i {
            font-size: 1.25rem;
        }

        .form-footer {
            text-align: center;
            margin-top: 1.5rem;
            padding-top: 1.5rem;
            border-top: 1px solid var(--glass-border);
        }

        .form-footer p {
            color: rgba(248, 250, 252, 0.6);
            margin-bottom: 0.5rem;
        }

        .form-footer a {
            color: var(--primary);
            text-decoration: none;
            font-weight: 600;
        }

        .form-footer a:hover {
            text-decoration: underline;
        }

        /* Error & Success States */
        .alert {
            padding: 1rem 1.25rem;
            border-radius: 12px;
            margin-bottom: 1.5rem;
            display: flex;
            align-items: flex-start;
            gap: 0.75rem;
        }

        .alert-danger {
            background: rgba(239, 68, 68, 0.15);
            border: 1px solid rgba(239, 68, 68, 0.3);
            color: #fca5a5;
        }

        .alert-info {
            background: rgba(99, 102, 241, 0.15);
            border: 1px solid rgba(99, 102, 241, 0.3);
            color: #a5b4fc;
        }

        .alert i {
            font-size: 1.25rem;
            margin-top: 2px;
        }

        .alert-content h5 {
            font-weight: 700;
            margin-bottom: 0.25rem;
        }

        .alert-actions {
            display: flex;
            gap: 0.75rem;
            margin-top: 0.75rem;
        }

        .alert-actions .btn {
            padding: 0.5rem 1rem;
            border-radius: 8px;
            font-size: 0.9rem;
            font-weight: 600;
            text-decoration: none;
            transition: all 0.3s ease;
        }

        .alert-actions .btn-primary {
            background: var(--primary);
            color: white;
        }

        .alert-actions .btn-outline {
            background: transparent;
            border: 1px solid var(--glass-border);
            color: var(--light);
        }

        .text-danger {
            color: #fca5a5;
            font-size: 0.85rem;
            margin-top: 0.25rem;
            display: block;
        }

        .trial-badge {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            background: rgba(16, 185, 129, 0.15);
            border: 1px solid rgba(16, 185, 129, 0.3);
            color: var(--secondary);
            padding: 0.5rem 1rem;
            border-radius: 50px;
            font-size: 0.9rem;
            margin-bottom: 1rem;
        }

        .back-link {
            position: absolute;
            top: 2rem;
            right: 2rem;
            color: rgba(248, 250, 252, 0.6);
            text-decoration: none;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 0.9rem;
            transition: color 0.3s ease;
        }

        .back-link:hover {
            color: var(--light);
        }
    </style>
</head>
<body>
    <div class="bg-pattern"></div>

    <a href="/" class="back-link">
        <i class="bi bi-arrow-right"></i>
        العودة للرئيسية
    </a>

    <div class="register-container">
        <!-- Left Side - Info -->
        <div class="register-side">
            <div class="side-content">
                <div class="side-logo">
                    <div class="side-logo-icon">ش</div>
                    <span class="side-logo-text">شاهين</span>
                </div>

                <h2 class="side-title">ابدأ رحلة الامتثال الذكي مع شاهين</h2>
                
                <p class="side-description">
                    سجّل الآن واحصل على 7 أيام تجريبية مجانية مع وصول كامل لجميع المميزات
                </p>

                <ul class="side-features">
                    <li>
                        <i class="bi bi-check"></i>
                        <span>96 سؤال ذكي لتحديد نطاق امتثالك</span>
                    </li>
                    <li>
                        <i class="bi bi-check"></i>
                        <span>أكثر من 13,500 ضابط رقابي جاهز</span>
                    </li>
                    <li>
                        <i class="bi bi-check"></i>
                        <span>130+ جهة تنظيمية سعودية</span>
                    </li>
                    <li>
                        <i class="bi bi-check"></i>
                        <span>7 سير عمل آلي مدمج</span>
                    </li>
                    <li>
                        <i class="bi bi-check"></i>
                        <span>لوحات تحكم تنفيذية فورية</span>
                    </li>
                    <li>
                        <i class="bi bi-check"></i>
                        <span>دعم فني متخصص</span>
                    </li>
                </ul>
            </div>
        </div>

        <!-- Right Side - Form -->
        <div class="register-form">
            <div class="form-header">
                <div class="trial-badge">
                    <i class="bi bi-gift"></i>
                    <span>7 أيام مجاناً</span>
                </div>
                <h1>إنشاء حساب جديد</h1>
                <p>أدخل بياناتك للبدء في رحلة الامتثال</p>
            </div>

            @if (isAlreadyRegistered)
            {
                <div class="alert alert-info">
                    <i class="bi bi-person-check"></i>
                    <div class="alert-content">
                        <h5>لديك حساب بالفعل! 👋</h5>
                        <p>يبدو أنك مسجل مسبقاً بهذا البريد الإلكتروني.</p>
                        <div class="alert-actions">
                            <a href="/Account/Login" class="btn btn-primary">
                                <i class="bi bi-box-arrow-in-right me-1"></i>تسجيل الدخول
                            </a>
                            <a href="/Account/ForgotPassword" class="btn btn-outline">نسيت كلمة المرور؟</a>
                        </div>
                    </div>
                </div>
            }
            else if (!ViewData.ModelState.IsValid)
            {
                <div class="alert alert-danger">
                    <i class="bi bi-exclamation-triangle"></i>
                    <div class="alert-content">
                        <h5>يرجى تصحيح الأخطاء</h5>
                        @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                        {
                            <p>@error.ErrorMessage</p>
                        }
                    </div>
                </div>
            }

            <form asp-action="Register" method="post">
                @Html.AntiForgeryToken()

                <div class="form-group">
                    <label asp-for="OrganizationName">
                        <i class="bi bi-building"></i>اسم المؤسسة
                    </label>
                    <input asp-for="OrganizationName" class="form-control" placeholder="مثال: شركة النجاح للتقنية" />
                    <span asp-validation-for="OrganizationName" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label asp-for="FullName">
                        <i class="bi bi-person"></i>الاسم الكامل
                    </label>
                    <input asp-for="FullName" class="form-control" placeholder="مثال: أحمد محمد العلي" />
                    <span asp-validation-for="FullName" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Email">
                        <i class="bi bi-envelope"></i>البريد الإلكتروني
                    </label>
                    <input asp-for="Email" type="email" class="form-control" placeholder="you@company.com" dir="ltr" style="text-align: left;" />
                    <span asp-validation-for="Email" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Password">
                        <i class="bi bi-lock"></i>كلمة المرور
                    </label>
                    <div class="input-group">
                        <input asp-for="Password" type="password" class="form-control" placeholder="12 حرف على الأقل" id="password" dir="ltr" style="text-align: left;" />
                        <button type="button" class="toggle-password" onclick="togglePassword()">
                            <i class="bi bi-eye" id="toggle-icon"></i>
                        </button>
                    </div>
                    <span asp-validation-for="Password" class="text-danger"></span>
                </div>

                <div class="form-check">
                    <input asp-for="AcceptTerms" type="checkbox" class="form-check-input" id="acceptTerms" />
                    <label asp-for="AcceptTerms" class="form-check-label">
                        أوافق على <a href="/Terms" target="_blank">الشروط والأحكام</a> و<a href="/Privacy" target="_blank">سياسة الخصوصية</a>
                    </label>
                </div>
                <span asp-validation-for="AcceptTerms" class="text-danger" style="margin-top: -1rem; margin-bottom: 1rem; display: block;"></span>

                <button type="submit" class="btn-submit">
                    <i class="bi bi-rocket-takeoff"></i>
                    <span>ابدأ التجربة المجانية</span>
                </button>

                <p style="text-align: center; margin-top: 1rem; font-size: 0.85rem; color: rgba(248,250,252,0.5);">
                    <i class="bi bi-shield-check me-1"></i>
                    بياناتك محمية ومشفرة بالكامل
                </p>
            </form>

            <div class="form-footer">
                <p>لديك حساب بالفعل؟</p>
                <a href="/Account/Login">تسجيل الدخول</a>
            </div>
            
            <!-- Powered by Dogan Consult -->
            <div style="text-align: center; margin-top: 2rem; padding-top: 1rem; border-top: 1px solid var(--glass-border);">
                <span style="color: rgba(248, 250, 252, 0.4); font-size: 0.8rem;">
                    Powered by 
                    <a href="https://www.doganconsult.com" target="_blank" rel="noopener" 
                       style="color: var(--primary); text-decoration: none; font-weight: 600;">
                        <i class="bi bi-lightning-charge-fill"></i> Dogan Consult
                    </a>
                </span>
            </div>
        </div>
    </div>

    <script>
        function togglePassword() {
            const input = document.getElementById('password');
            const icon = document.getElementById('toggle-icon');
            if (input.type === 'password') {
                input.type = 'text';
                icon.className = 'bi bi-eye-slash';
            } else {
                input.type = 'password';
                icon.className = 'bi bi-eye';
            }
        }

        // ========== DIAGNOSTICS & AI AGENT INTEGRATION ==========
        (function() {
            const sessionId = localStorage.getItem('shahin_session') || 
                              'sess_' + Math.random().toString(36).substr(2, 9);
            localStorage.setItem('shahin_session', sessionId);

            // Log trial page view
            fetch('/api/diagnostics/visitor', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({
                    sessionId: sessionId,
                    page: '/trial',
                    action: 'trial_page_view',
                    metadata: {
                        referrer: document.referrer,
                        screenWidth: window.innerWidth,
                        language: navigator.language
                    }
                })
            }).catch(() => {});

            // Global error handler
            window.onerror = function(message, source, lineno, colno, error) {
                fetch('/api/diagnostics/error', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({
                        sessionId: sessionId,
                        page: '/trial',
                        errorMessage: message,
                        stackTrace: error?.stack || `${source}:${lineno}:${colno}`,
                        errorType: 'JavaScriptError',
                        metadata: { source, lineno, colno }
                    })
                }).catch(() => {});
                return false;
            };

            // Track form interactions
            const form = document.querySelector('form');
            if (form) {
                form.addEventListener('submit', function(e) {
                    fetch('/api/diagnostics/visitor', {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify({
                            sessionId: sessionId,
                            page: '/trial',
                            action: 'trial_form_submit',
                            metadata: {}
                        })
                    }).catch(() => {});
                });

                // Track field focus
                form.querySelectorAll('input').forEach(input => {
                    input.addEventListener('focus', function() {
                        fetch('/api/diagnostics/visitor', {
                            method: 'POST',
                            headers: { 'Content-Type': 'application/json' },
                            body: JSON.stringify({
                                sessionId: sessionId,
                                page: '/trial',
                                action: 'field_focus',
                                metadata: { field: this.name || this.id }
                            })
                        }).catch(() => {});
                    });
                });
            }

            // AI Agent - Chat assistance (show after 10 seconds)
            setTimeout(function() {
                const aiAssistant = document.createElement('div');
                aiAssistant.id = 'ai-assistant';
                aiAssistant.innerHTML = `
                    <div id="ai-bubble" style="
                        position: fixed;
                        bottom: 20px;
                        left: 20px;
                        width: 60px;
                        height: 60px;
                        background: linear-gradient(135deg, var(--primary), var(--secondary));
                        border-radius: 50%;
                        box-shadow: 0 4px 20px rgba(99, 102, 241, 0.4);
                        cursor: pointer;
                        display: flex;
                        align-items: center;
                        justify-content: center;
                        transition: all 0.3s ease;
                        z-index: 1000;
                    ">
                        <i class="bi bi-robot" style="color: white; font-size: 24px;"></i>
                    </div>
                    <div id="ai-chat" style="
                        display: none;
                        position: fixed;
                        bottom: 90px;
                        left: 20px;
                        width: 320px;
                        max-height: 400px;
                        background: var(--glass);
                        backdrop-filter: blur(20px);
                        border: 1px solid var(--glass-border);
                        border-radius: 16px;
                        box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
                        z-index: 1000;
                        overflow: hidden;
                    ">
                        <div style="padding: 16px; border-bottom: 1px solid var(--glass-border); display: flex; justify-content: space-between; align-items: center;">
                            <div>
                                <strong style="color: var(--light);">🤖 مساعد شاهين</strong>
                                <p style="margin: 0; font-size: 0.8rem; color: rgba(248, 250, 252, 0.6);">مدعوم بالذكاء الاصطناعي</p>
                            </div>
                            <button onclick="toggleAiChat()" style="background: none; border: none; color: var(--light); cursor: pointer;">
                                <i class="bi bi-x-lg"></i>
                            </button>
                        </div>
                        <div id="ai-messages" style="padding: 16px; max-height: 250px; overflow-y: auto;">
                            <div style="background: rgba(99, 102, 241, 0.2); padding: 12px; border-radius: 12px; margin-bottom: 8px;">
                                <p style="margin: 0; font-size: 0.9rem;">مرحباً! 👋 أنا مساعد شاهين. هل تحتاج مساعدة في التسجيل؟</p>
                            </div>
                        </div>
                        <div style="padding: 12px; border-top: 1px solid var(--glass-border); display: flex; gap: 8px;">
                            <input id="ai-input" type="text" placeholder="اكتب سؤالك..." style="
                                flex: 1;
                                padding: 8px 12px;
                                background: var(--dark);
                                border: 1px solid var(--glass-border);
                                border-radius: 8px;
                                color: var(--light);
                                font-family: inherit;
                            " onkeypress="if(event.key==='Enter')sendAiMessage()">
                            <button onclick="sendAiMessage()" style="
                                padding: 8px 16px;
                                background: var(--primary);
                                border: none;
                                border-radius: 8px;
                                color: white;
                                cursor: pointer;
                            ">
                                <i class="bi bi-send"></i>
                            </button>
                        </div>
                    </div>
                `;
                document.body.appendChild(aiAssistant);
                
                document.getElementById('ai-bubble').addEventListener('click', toggleAiChat);
                
                // Log AI assistant shown
                fetch('/api/diagnostics/visitor', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({
                        sessionId: sessionId,
                        page: '/trial',
                        action: 'ai_assistant_shown',
                        metadata: {}
                    })
                }).catch(() => {});
            }, 10000);
        })();

        function toggleAiChat() {
            const chat = document.getElementById('ai-chat');
            const bubble = document.getElementById('ai-bubble');
            if (chat.style.display === 'none') {
                chat.style.display = 'block';
                bubble.style.transform = 'scale(0.9)';
            } else {
                chat.style.display = 'none';
                bubble.style.transform = 'scale(1)';
            }
        }

        async function sendAiMessage() {
            const input = document.getElementById('ai-input');
            const messages = document.getElementById('ai-messages');
            const message = input.value.trim();
            if (!message) return;

            // Add user message
            messages.innerHTML += `
                <div style="background: var(--dark); padding: 12px; border-radius: 12px; margin-bottom: 8px; margin-right: 20px;">
                    <p style="margin: 0; font-size: 0.9rem;">${message}</p>
                </div>
            `;
            input.value = '';
            messages.scrollTop = messages.scrollHeight;

            // Log AI interaction
            fetch('/api/diagnostics/visitor', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({
                    sessionId: localStorage.getItem('shahin_session'),
                    page: '/trial',
                    action: 'ai_chat_message',
                    metadata: { messageLength: message.length }
                })
            }).catch(() => {});

            // Show typing indicator
            const typingId = 'typing-' + Date.now();
            messages.innerHTML += `
                <div id="${typingId}" style="background: rgba(99, 102, 241, 0.2); padding: 12px; border-radius: 12px; margin-bottom: 8px;">
                    <p style="margin: 0; font-size: 0.9rem;">جاري الكتابة...</p>
                </div>
            `;
            messages.scrollTop = messages.scrollHeight;

            try {
                // Call AI agent API (using GET to avoid POST issues)
                const encodedMessage = encodeURIComponent(message);
                const response = await fetch(`/api/agent/chat/public?message=${encodedMessage}&context=trial_registration`);
                
                let aiResponse = 'شكراً لتواصلك! يمكنك التسجيل مجاناً لمدة 7 أيام. فريقنا سيتواصل معك قريباً.';
                
                if (response.ok) {
                    const data = await response.json();
                    if (data.response) aiResponse = data.response;
                }

                // Remove typing and add response
                document.getElementById(typingId)?.remove();
                messages.innerHTML += `
                    <div style="background: rgba(99, 102, 241, 0.2); padding: 12px; border-radius: 12px; margin-bottom: 8px;">
                        <p style="margin: 0; font-size: 0.9rem;">${aiResponse}</p>
                    </div>
                `;
                messages.scrollTop = messages.scrollHeight;
            } catch (e) {
                document.getElementById(typingId)?.remove();
                messages.innerHTML += `
                    <div style="background: rgba(99, 102, 241, 0.2); padding: 12px; border-radius: 12px; margin-bottom: 8px;">
                        <p style="margin: 0; font-size: 0.9rem;">شكراً لتواصلك! سجّل الآن واستمتع بـ 7 أيام مجانية.</p>
                    </div>
                `;
                messages.scrollTop = messages.scrollHeight;
            }
        }
    </script>
</body>
</html>
